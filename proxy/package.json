{
  "name": "proxy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=development node --loader @swc-node/register/esm --no-warnings index.ts",
    "start": "NODE_ENV=production node --loader @swc-node/register/esm --no-warnings index.ts"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "dotenv-flow": "^4.0.1"
  },
  "dependencies": {
    "music-metadata": "^7.14.0",
    "node-fetch": "^3.3.2"
  }
}
